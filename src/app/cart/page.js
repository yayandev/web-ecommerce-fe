import CartView from "@/components/pages/cart/CartView";

export const metadata = {
  title: "Cart",
};

const CartPage = () => {
  return (
    <>
      <CartView />
    </>
  );
};

export default CartPage;
